        ;;  cursors.s
        ;;  
        ;;  cursors_font
        ;; 
        ;;  notes: see font.h for format details
        ;;  
        ;;  MIT License (see: LICENSE)
        ;;  copyright(c) 2021 tomaz stih
        ;;  
        ;;  10.10.2021   tstih
        .module cursors_font

        .globl _cursors_font

        .area _CODE
_cursors_font::
        ;; font header
        .db 0                   ; font flags
        .db 15                  ; width (max width for proportional)
        .db 15                  ; height
        .db 65                  ; first ascii
        .db 67                  ; last ascii

        ;; glpyh offsets
        .dw 0x000B, 0x0058, 0x0088

        ;; ascii 65: 'A'
        .db 32                  ; class(bits 5-7)
        .db 15                  ; width
        .db 15                  ; height
        .db 73                  ; # moves
        .db 10                  ; x origin
        .db 11                  ; y origin
        .db 13                  ; move dx=0, dy=-1, color=back (clear)
        .db 35                  ; move dx=-1, dy=0, color=back (clear)
        .db 13                  ; move dx=0, dy=-1, color=back (clear)
        .db 97                  ; move dx=3, dy=0, color=back (clear)
        .db 21                  ; move dx=0, dy=-2, color=back (clear)
        .db 35                  ; move dx=-1, dy=0, color=back (clear)
        .db 47                  ; move dx=-1, dy=-1, color=back (clear)
        .db 33                  ; move dx=1, dy=0, color=back (clear)
        .db 47                  ; move dx=-1, dy=-1, color=back (clear)
        .db 42                  ; move dx=-1, dy=1, color=none (move only!)
        .db 128                 ; move dx=0, dy=0, color=fore (set)
        .db 226                 ; move dx=-3, dy=0, color=fore (set)
        .db 194                 ; move dx=-2, dy=0, color=fore (set)
        .db 140                 ; move dx=0, dy=-1, color=fore (set)
        .db 224                 ; move dx=3, dy=0, color=fore (set)
        .db 160                 ; move dx=1, dy=0, color=fore (set)
        .db 254                 ; move dx=-3, dy=-3, color=fore (set)
        .db 162                 ; move dx=-1, dy=0, color=fore (set)
        .db 144                 ; move dx=0, dy=2, color=fore (set)
        .db 192                 ; move dx=2, dy=0, color=fore (set)
        .db 174                 ; move dx=-1, dy=-1, color=fore (set)
        .db 44                  ; move dx=1, dy=-1, color=none (move only!)
        .db 1                   ; move dx=0, dy=0, color=back (clear)
        .db 121                 ; move dx=3, dy=3, color=back (clear)
        .db 13                  ; move dx=0, dy=-1, color=back (clear)
        .db 127                 ; move dx=-3, dy=-3, color=back (clear)
        .db 47                  ; move dx=-1, dy=-1, color=back (clear)
        .db 9                   ; move dx=0, dy=1, color=back (clear)
        .db 47                  ; move dx=-1, dy=-1, color=back (clear)
        .db 35                  ; move dx=-1, dy=0, color=back (clear)
        .db 25                  ; move dx=0, dy=3, color=back (clear)
        .db 25                  ; move dx=0, dy=3, color=back (clear)
        .db 9                   ; move dx=0, dy=1, color=back (clear)
        .db 32                  ; move dx=1, dy=0, color=none (move only!)
        .db 224                 ; move dx=3, dy=0, color=fore (set)
        .db 224                 ; move dx=3, dy=0, color=fore (set)
        .db 136                 ; move dx=0, dy=1, color=fore (set)
        .db 226                 ; move dx=-3, dy=0, color=fore (set)
        .db 226                 ; move dx=-3, dy=0, color=fore (set)
        .db 128                 ; move dx=0, dy=0, color=fore (set)
        .db 34                  ; move dx=-1, dy=0, color=none (move only!)
        .db 1                   ; move dx=0, dy=0, color=back (clear)
        .db 25                  ; move dx=0, dy=3, color=back (clear)
        .db 9                   ; move dx=0, dy=1, color=back (clear)
        .db 65                  ; move dx=2, dy=0, color=back (clear)
        .db 13                  ; move dx=0, dy=-1, color=back (clear)
        .db 45                  ; move dx=1, dy=-1, color=back (clear)
        .db 34                  ; move dx=-1, dy=0, color=none (move only!)
        .db 162                 ; move dx=-1, dy=0, color=fore (set)
        .db 140                 ; move dx=0, dy=-1, color=fore (set)
        .db 224                 ; move dx=3, dy=0, color=fore (set)
        .db 160                 ; move dx=1, dy=0, color=fore (set)
        .db 170                 ; move dx=-1, dy=1, color=fore (set)
        .db 160                 ; move dx=1, dy=0, color=fore (set)
        .db 168                 ; move dx=1, dy=1, color=fore (set)
        .db 162                 ; move dx=-1, dy=0, color=fore (set)
        .db 34                  ; move dx=-1, dy=0, color=none (move only!)
        .db 35                  ; move dx=-1, dy=0, color=back (clear)
        .db 81                  ; move dx=2, dy=2, color=back (clear)
        .db 35                  ; move dx=-1, dy=0, color=back (clear)
        .db 41                  ; move dx=1, dy=1, color=back (clear)
        .db 32                  ; move dx=1, dy=0, color=none (move only!)
        .db 160                 ; move dx=1, dy=0, color=fore (set)
        .db 214                 ; move dx=-2, dy=-2, color=fore (set)
        .db 160                 ; move dx=1, dy=0, color=fore (set)
        .db 32                  ; move dx=1, dy=0, color=none (move only!)
        .db 33                  ; move dx=1, dy=0, color=back (clear)
        .db 17                  ; move dx=0, dy=2, color=back (clear)
        .db 46                  ; move dx=-1, dy=-1, color=none (move only!)
        .db 82                  ; move dx=-2, dy=2, color=none (move only!)
        .db 97                  ; move dx=3, dy=0, color=back (clear)
        ;; ascii 66: 'B'
        .db 32                  ; class(bits 5-7)
        .db 15                  ; width
        .db 15                  ; height
        .db 44                  ; # moves
        .db 6                   ; x origin
        .db 3                   ; y origin
        .db 13                  ; move dx=0, dy=-1, color=back (clear)
        .db 67                  ; move dx=-2, dy=0, color=back (clear)
        .db 21                  ; move dx=0, dy=-2, color=back (clear)
        .db 97                  ; move dx=3, dy=0, color=back (clear)
        .db 9                   ; move dx=0, dy=1, color=back (clear)
        .db 65                  ; move dx=2, dy=0, color=back (clear)
        .db 13                  ; move dx=0, dy=-1, color=back (clear)
        .db 97                  ; move dx=3, dy=0, color=back (clear)
        .db 17                  ; move dx=0, dy=2, color=back (clear)
        .db 67                  ; move dx=-2, dy=0, color=back (clear)
        .db 9                   ; move dx=0, dy=1, color=back (clear)
        .db 35                  ; move dx=-1, dy=0, color=back (clear)
        .db 47                  ; move dx=-1, dy=-1, color=back (clear)
        .db 43                  ; move dx=-1, dy=1, color=back (clear)
        .db 25                  ; move dx=0, dy=3, color=back (clear)
        .db 25                  ; move dx=0, dy=3, color=back (clear)
        .db 25                  ; move dx=0, dy=3, color=back (clear)
        .db 41                  ; move dx=1, dy=1, color=back (clear)
        .db 45                  ; move dx=1, dy=-1, color=back (clear)
        .db 29                  ; move dx=0, dy=-3, color=back (clear)
        .db 29                  ; move dx=0, dy=-3, color=back (clear)
        .db 21                  ; move dx=0, dy=-2, color=back (clear)
        .db 34                  ; move dx=-1, dy=0, color=none (move only!)
        .db 128                 ; move dx=0, dy=0, color=fore (set)
        .db 152                 ; move dx=0, dy=3, color=fore (set)
        .db 152                 ; move dx=0, dy=3, color=fore (set)
        .db 136                 ; move dx=0, dy=1, color=fore (set)
        .db 80                  ; move dx=2, dy=2, color=none (move only!)
        .db 64                  ; move dx=2, dy=0, color=none (move only!)
        .db 9                   ; move dx=0, dy=1, color=back (clear)
        .db 87                  ; move dx=-2, dy=-2, color=back (clear)
        .db 9                   ; move dx=0, dy=1, color=back (clear)
        .db 43                  ; move dx=-1, dy=1, color=back (clear)
        .db 67                  ; move dx=-2, dy=0, color=back (clear)
        .db 47                  ; move dx=-1, dy=-1, color=back (clear)
        .db 13                  ; move dx=0, dy=-1, color=back (clear)
        .db 43                  ; move dx=-1, dy=1, color=back (clear)
        .db 35                  ; move dx=-1, dy=0, color=back (clear)
        .db 17                  ; move dx=0, dy=2, color=back (clear)
        .db 97                  ; move dx=3, dy=0, color=back (clear)
        .db 64                  ; move dx=2, dy=0, color=none (move only!)
        .db 97                  ; move dx=3, dy=0, color=back (clear)
        ;; ascii 67: 'C'
        .db 32                  ; class(bits 5-7)
        .db 15                  ; width
        .db 15                  ; height
        .db 111                 ; # moves
        .db 8                   ; x origin
        .db 5                   ; y origin
        .db 162                 ; move dx=-1, dy=0, color=fore (set)
        .db 34                  ; move dx=-1, dy=0, color=none (move only!)
        .db 35                  ; move dx=-1, dy=0, color=back (clear)
        .db 13                  ; move dx=0, dy=-1, color=back (clear)
        .db 97                  ; move dx=3, dy=0, color=back (clear)
        .db 65                  ; move dx=2, dy=0, color=back (clear)
        .db 43                  ; move dx=-1, dy=1, color=back (clear)
        .db 33                  ; move dx=1, dy=0, color=back (clear)
        .db 43                  ; move dx=-1, dy=1, color=back (clear)
        .db 99                  ; move dx=-3, dy=0, color=back (clear)
        .db 121                 ; move dx=3, dy=3, color=back (clear)
        .db 99                  ; move dx=-3, dy=0, color=back (clear)
        .db 85                  ; move dx=2, dy=-2, color=back (clear)
        .db 40                  ; move dx=1, dy=1, color=none (move only!)
        .db 208                 ; move dx=2, dy=2, color=fore (set)
        .db 136                 ; move dx=0, dy=1, color=fore (set)
        .db 34                  ; move dx=-1, dy=0, color=none (move only!)
        .db 1                   ; move dx=0, dy=0, color=back (clear)
        .db 47                  ; move dx=-1, dy=-1, color=back (clear)
        .db 33                  ; move dx=1, dy=0, color=back (clear)
        .db 45                  ; move dx=1, dy=-1, color=back (clear)
        .db 47                  ; move dx=-1, dy=-1, color=back (clear)
        .db 33                  ; move dx=1, dy=0, color=back (clear)
        .db 41                  ; move dx=1, dy=1, color=back (clear)
        .db 17                  ; move dx=0, dy=2, color=back (clear)
        .db 33                  ; move dx=1, dy=0, color=back (clear)
        .db 25                  ; move dx=0, dy=3, color=back (clear)
        .db 35                  ; move dx=-1, dy=0, color=back (clear)
        .db 47                  ; move dx=-1, dy=-1, color=back (clear)
        .db 99                  ; move dx=-3, dy=0, color=back (clear)
        .db 99                  ; move dx=-3, dy=0, color=back (clear)
        .db 35                  ; move dx=-1, dy=0, color=back (clear)
        .db 8                   ; move dx=0, dy=1, color=none (move only!)
        .db 128                 ; move dx=0, dy=0, color=fore (set)
        .db 224                 ; move dx=3, dy=0, color=fore (set)
        .db 224                 ; move dx=3, dy=0, color=fore (set)
        .db 160                 ; move dx=1, dy=0, color=fore (set)
        .db 172                 ; move dx=1, dy=-1, color=fore (set)
        .db 140                 ; move dx=0, dy=-1, color=fore (set)
        .db 226                 ; move dx=-3, dy=0, color=fore (set)
        .db 226                 ; move dx=-3, dy=0, color=fore (set)
        .db 162                 ; move dx=-1, dy=0, color=fore (set)
        .db 172                 ; move dx=1, dy=-1, color=fore (set)
        .db 224                 ; move dx=3, dy=0, color=fore (set)
        .db 174                 ; move dx=-1, dy=-1, color=fore (set)
        .db 162                 ; move dx=-1, dy=0, color=fore (set)
        .db 34                  ; move dx=-1, dy=0, color=none (move only!)
        .db 35                  ; move dx=-1, dy=0, color=back (clear)
        .db 9                   ; move dx=0, dy=1, color=back (clear)
        .db 42                  ; move dx=-1, dy=1, color=none (move only!)
        .db 170                 ; move dx=-1, dy=1, color=fore (set)
        .db 140                 ; move dx=0, dy=-1, color=fore (set)
        .db 172                 ; move dx=1, dy=-1, color=fore (set)
        .db 140                 ; move dx=0, dy=-1, color=fore (set)
        .db 212                 ; move dx=2, dy=-2, color=fore (set)
        .db 140                 ; move dx=0, dy=-1, color=fore (set)
        .db 214                 ; move dx=-2, dy=-2, color=fore (set)
        .db 140                 ; move dx=0, dy=-1, color=fore (set)
        .db 174                 ; move dx=-1, dy=-1, color=fore (set)
        .db 224                 ; move dx=3, dy=0, color=fore (set)
        .db 224                 ; move dx=3, dy=0, color=fore (set)
        .db 224                 ; move dx=3, dy=0, color=fore (set)
        .db 170                 ; move dx=-1, dy=1, color=fore (set)
        .db 136                 ; move dx=0, dy=1, color=fore (set)
        .db 210                 ; move dx=-2, dy=2, color=fore (set)
        .db 32                  ; move dx=1, dy=0, color=none (move only!)
        .db 33                  ; move dx=1, dy=0, color=back (clear)
        .db 45                  ; move dx=1, dy=-1, color=back (clear)
        .db 35                  ; move dx=-1, dy=0, color=back (clear)
        .db 85                  ; move dx=2, dy=-2, color=back (clear)
        .db 35                  ; move dx=-1, dy=0, color=back (clear)
        .db 45                  ; move dx=1, dy=-1, color=back (clear)
        .db 21                  ; move dx=0, dy=-2, color=back (clear)
        .db 35                  ; move dx=-1, dy=0, color=back (clear)
        .db 13                  ; move dx=0, dy=-1, color=back (clear)
        .db 99                  ; move dx=-3, dy=0, color=back (clear)
        .db 99                  ; move dx=-3, dy=0, color=back (clear)
        .db 99                  ; move dx=-3, dy=0, color=back (clear)
        .db 43                  ; move dx=-1, dy=1, color=back (clear)
        .db 40                  ; move dx=1, dy=1, color=none (move only!)
        .db 128                 ; move dx=0, dy=0, color=fore (set)
        .db 172                 ; move dx=1, dy=-1, color=fore (set)
        .db 224                 ; move dx=3, dy=0, color=fore (set)
        .db 224                 ; move dx=3, dy=0, color=fore (set)
        .db 160                 ; move dx=1, dy=0, color=fore (set)
        .db 168                 ; move dx=1, dy=1, color=fore (set)
        .db 34                  ; move dx=-1, dy=0, color=none (move only!)
        .db 99                  ; move dx=-3, dy=0, color=back (clear)
        .db 99                  ; move dx=-3, dy=0, color=back (clear)
        .db 35                  ; move dx=-1, dy=0, color=back (clear)
        .db 47                  ; move dx=-1, dy=-1, color=back (clear)
        .db 43                  ; move dx=-1, dy=1, color=back (clear)
        .db 17                  ; move dx=0, dy=2, color=back (clear)
        .db 33                  ; move dx=1, dy=0, color=back (clear)
        .db 17                  ; move dx=0, dy=2, color=back (clear)
        .db 33                  ; move dx=1, dy=0, color=back (clear)
        .db 17                  ; move dx=0, dy=2, color=back (clear)
        .db 45                  ; move dx=1, dy=-1, color=back (clear)
        .db 9                   ; move dx=0, dy=1, color=back (clear)
        .db 43                  ; move dx=-1, dy=1, color=back (clear)
        .db 35                  ; move dx=-1, dy=0, color=back (clear)
        .db 17                  ; move dx=0, dy=2, color=back (clear)
        .db 35                  ; move dx=-1, dy=0, color=back (clear)
        .db 25                  ; move dx=0, dy=3, color=back (clear)
        .db 33                  ; move dx=1, dy=0, color=back (clear)
        .db 9                   ; move dx=0, dy=1, color=back (clear)
        .db 97                  ; move dx=3, dy=0, color=back (clear)
        .db 97                  ; move dx=3, dy=0, color=back (clear)
        .db 97                  ; move dx=3, dy=0, color=back (clear)
